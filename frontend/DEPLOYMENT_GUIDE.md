# دليل النشر - Deployment Guide

هذا الدليل يوضح كيفية رفع المشروع أونلاين على منصات مختلفة.

## 📋 المتطلبات

- Node.js >= 20.0.0
- npm أو yarn
- حساب على منصة النشر (Vercel, Netlify, أو غيرها)

## 🚀 النشر على Vercel

### الطريقة 1: من خلال GitHub (موصى بها)

1. **ادفع المشروع إلى GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/yourusername/your-repo.git
   git push -u origin main
   ```

2. **سجل دخول إلى Vercel:**
   - اذهب إلى [vercel.com](https://vercel.com)
   - سجل دخول بحساب GitHub

3. **استيراد المشروع:**
   - اضغط على "New Project"
   - اختر المستودع الخاص بك
   - Vercel سيكتشف تلقائياً أنه مشروع Vite

4. **إعداد متغيرات البيئة:**
   - في صفحة إعدادات المشروع
   - اذهب إلى "Environment Variables"
   - أضف:
     ```
     VITE_API_URL=https://your-backend-api.com/api
     ```

5. **النشر:**
   - اضغط "Deploy"
   - انتظر حتى يكتمل البناء
   - سيتم توفير رابط للموقع

### الطريقة 2: من خلال Vercel CLI

```bash
# تثبيت Vercel CLI
npm i -g vercel

# تسجيل الدخول
vercel login

# النشر
vercel

# النشر للإنتاج
vercel --prod
```

## 🌐 النشر على Netlify

### الطريقة 1: من خلال GitHub

1. **ادفع المشروع إلى GitHub** (كما هو موضح أعلاه)

2. **سجل دخول إلى Netlify:**
   - اذهب إلى [netlify.com](https://netlify.com)
   - سجل دخول بحساب GitHub

3. **استيراد المشروع:**
   - اضغط على "New site from Git"
   - اختر المستودع الخاص بك
   - Netlify سيكتشف تلقائياً الإعدادات من `netlify.toml`

4. **إعداد متغيرات البيئة:**
   - في صفحة إعدادات المشروع
   - اذهب إلى "Site settings" > "Environment variables"
   - أضف:
     ```
     VITE_API_URL=https://your-backend-api.com/api
     ```

5. **النشر:**
   - اضغط "Deploy site"
   - انتظر حتى يكتمل البناء

### الطريقة 2: من خلال Netlify CLI

```bash
# تثبيت Netlify CLI
npm i -g netlify-cli

# تسجيل الدخول
netlify login

# النشر
netlify deploy

# النشر للإنتاج
netlify deploy --prod
```

## 🔧 إعداد متغيرات البيئة

قبل النشر، تأكد من إعداد متغيرات البيئة التالية:

### في Vercel:
1. اذهب إلى Project Settings > Environment Variables
2. أضف:
   - `VITE_API_URL`: رابط API الخاص بك

### في Netlify:
1. اذهب إلى Site settings > Environment variables
2. أضف:
   - `VITE_API_URL`: رابط API الخاص بك

## 📝 تحديث ملفات التكوين

### تحديث `vercel.json`:
قم بتحديث رابط API في `vercel.json`:
```json
{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://your-backend-api.com/api/$1"
    }
  ]
}
```

### تحديث `netlify.toml`:
قم بتحديث رابط API في `netlify.toml`:
```toml
[[redirects]]
  from = "/api/*"
  to = "https://your-backend-api.com/api/:splat"
```

## 🏗️ البناء المحلي

قبل النشر، يمكنك اختبار البناء محلياً:

```bash
# بناء المشروع
npm run build

# معاينة البناء
npm run preview
```

## ✅ التحقق من النشر

بعد النشر، تأكد من:

1. ✅ الموقع يعمل بشكل صحيح
2. ✅ الاتصال بـ API يعمل
3. ✅ جميع الصفحات قابلة للوصول
4. ✅ التصميم المتجاوب يعمل على جميع الأجهزة

## 🔒 الأمان

- لا ترفع ملفات `.env` إلى GitHub
- استخدم متغيرات البيئة في منصة النشر
- تأكد من أن API الخاص بك يدعم CORS
- استخدم HTTPS دائماً

## 🐛 حل المشاكل

### المشكلة: الموقع لا يعمل بعد النشر
- تحقق من أن البناء نجح
- تحقق من متغيرات البيئة
- تحقق من console للأخطاء

### المشكلة: API لا يعمل
- تحقق من رابط API في متغيرات البيئة
- تحقق من إعدادات CORS في API
- تحقق من أن API متاح على الإنترنت

### المشكلة: الصفحات تعرض 404
- تحقق من إعدادات الـ rewrites/redirects
- تأكد من أن جميع المسارات تعيد التوجيه إلى `index.html`

## 📞 الدعم

إذا واجهت أي مشاكل، راجع:
- [Vercel Documentation](https://vercel.com/docs)
- [Netlify Documentation](https://docs.netlify.com)

---

**ملاحظة:** تأكد من تحديث رابط API في جميع ملفات التكوين قبل النشر!

